const assert = require('assert');
const rewire = require('rewire');
const sinon = require('sinon');
const swagger = rewire('../../lib/middleware/swagger');

describe('The swagger middleware', function() {
  describe('watcher', function() {
    xit('should create a new swagger router on file change', function() {

    });

    xit('should watch multiple files and directories', function() {

    });

    xit('should not create a new router on unmodified file', function() {

    });
  });
})
;
