#!/usr/bin/env node

let dispatch = require('../lib/dispatch');
let createApp = require('../createDefaultApp');

dispatch(process.argv.slice(2), createApp).catch(function(err) {
  console.log(err.stack);
  process.exit(1);
});
